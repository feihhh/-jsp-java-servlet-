package per.fei.core.Condition;

import per.fei.core.teacher.Teacher;

/**
 * 查询条件类，继承Teacher类（直接用teacher类也可以查，只是刚开始想的有问题，后来就没改了）
 */
public class Condition extends Teacher {
    public Condition() {}
}
